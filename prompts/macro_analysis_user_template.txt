下面是今天的宏观分析结构化数据包。请严格基于该 JSON 内容生成每日宏观观察报告。

你必须生成一句话总判断；解释最重要的维度、2到4个组合关系和1到3个背离；从候选状态中选择1到3个状态；给出观察指标、数据置信度、风险提示和宏观学习笔记。

不要重新计算分数，不要修改 score、label、confidence，不要引入 JSON 中没有的数据，不要做投资建议。输出必须是合法 JSON，字段为：one_sentence_summary、macro_state_labels、overall_tone、dimension_interpretations、key_relation_interpretations、top_divergences、main_judgement、next_watchlist、risk_notes、data_confidence、learning_note。

数据包如下：

{{DAILY_MACRO_PAYLOAD_JSON}}
